In [[OAuth]] the scope defines the operations a user can perform on an [[Application Programming Interface|API]] ([[Authorization]]).

[[Client|Clients]] request scopes through a parameter in the access request.
