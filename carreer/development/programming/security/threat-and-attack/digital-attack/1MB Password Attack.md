A [[Digital Attack]] where [[Attacker|Attackers]] achieve [[Denial of Service|DoS]] by using a oversized [[Password]] as a way to force the [[Server]] to [[Hashing|Hash]] a single [[Request]], forcing [[Centrral Processing Unit|CPU]] to 100%

# Mitigation

Set he maximum password length to 128 or 256 characteres