A  technique to call [[Functions]]s which return [[Functions]]s which should be called (mutually [[Recursion|recursive]]) `foo(n)()()()....` in a way you don't need to manually call the latter `trampoline(foo, n)`

#learn 