Both predict the [[Probability]] of an [[maths/probability-theory/Event]] occurring.

They are mathematically equivalent but the logit model is more elegant: `log(odd) = LINEAR_REGRESSION`

---

It uses [[Logistic Regression]]
